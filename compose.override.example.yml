services:
  ollama:
    # Optional spaeter fuer echte iGPU-/GPU-Tests auf dem NAS:
    # devices:
    #   - /dev/dri:/dev/dri
    environment:
      OLLAMA_NUM_PARALLEL: "1"

  paperless-ai-web:
    environment:
      PAPERLESS_AI_WEB_THEME: "default"

